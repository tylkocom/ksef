---
title: Generowanie kodu sync
description: Utrzymuj wygenerowane klienty sync na podstawie modułów async.
---

Moduły async są źródłem prawdy dla większości implementacji endpointów,
klientów, middleware i serwisów. Moduły sync wymienione w `scripts/gen_sync.py`
są generowane z tych źródeł async.

Wygenerowane pliki zaczynają się od:

```python
# Generated by scripts/gen_sync.py from src/ksef2/clients/async_auth.py.
# DO NOT EDIT BY HAND - edit the async source and run `just gen-sync`.
```

## Zmień wygenerowaną parę

1. Edytuj moduł źródłowy async.

   ```bash
   $EDITOR src/ksef2/clients/async_auth.py
   ```

2. Wygeneruj odpowiadający moduł sync.

   ```bash
   just gen-sync
   ```

   Dla jednej pary użyj generatora bezpośrednio:

   ```bash
   uv run python scripts/gen_sync.py --only src/ksef2/clients/async_auth.py
   ```

3. Sprawdź diff źródła async i wygenerowanego targetu sync.

   ```bash
   git diff -- src/ksef2/clients/async_auth.py src/ksef2/clients/auth.py
   ```

4. Uruchom check generacji przed PR.

   ```bash
   just check-gen-sync
   ```

`just release-check` też uruchamia `check-gen-sync`, więc nieaktualne pliki
generated blokują release.

## Sprawdź jedną parę

Użyj `--only` ze źródłem async albo targetem sync:

```bash
uv run python scripts/gen_sync.py --check --diff --only src/ksef2/clients/auth.py
```

To pomaga przy pracy nad jedną parą, gdy chcesz zobaczyć krótki diff.

## Granice generatora

Nie każda para sync/async jest generowana. Część modułów jest celowo wspólna
albo strukturalnie inna:

- `src/ksef2/core/polling.py` zawiera wspólne helpery pollingu sync i async.
- `src/ksef2/core/response_errors.py` zawiera wspólne mapowanie błędów odpowiedzi.
- `src/ksef2/clients/base.py` i `src/ksef2/clients/async_base.py` różnią się
  własnością klienta głównego i cyklem życia.
- `src/ksef2/core/http.py` i `src/ksef2/core/async_http.py` różnią się
  własnością transportu i zamykaniem.

Lista par i wyjątków znajduje się w `scripts/gen_sync.py`. Aktualizuj ją, gdy
moduł staje się generated albo celowo divergent.

## Co sprawdza release gate

`just release-check` uruchamia:

```bash
just lint
just format-check
just check-ksef-api-version
just check-gen-sync
just typecheck
just test
just test-runtime-checks
uv build
```

Dla generacji sync oznacza to, że:

- wygenerowane targety zgadzają się z transformacją źródeł async;
- wygenerowane pliki nadal formatują się konfiguracją repo;
- publiczne powierzchnie sync i async są pilnowane przez testy parytetu;
- paczka buduje się z wygenerowanych plików zapisanych w repo.

## Referencja

- [Konfiguracja klienta](../workflows/client-setup.mdx)
- [Low-level API](../raw/overview.md)
